# Agent Instructions

- After completing any feature or bug fix in this repository, automatically package/deploy it locally so the user can run it with the `lato` command.
- Standard local deployment command: `cargo install --path .` from the repository root.
- Run relevant tests before deploying when practical.
