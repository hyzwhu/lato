# TUI continuity implementation plan

**Goal:** Keep interactive operations in TUI and make the cursor follow visible input, then publish beta 2.

**Architecture:** A terminal guard spans startup and chat. Channel-backed asynchronous dialogs reuse configuration/auth services. A shared SwitchableModelStream preserves the current session during model changes.

**Tech stack:** Rust, Tokio, Crossterm, Ratatui.

- [x] Add a Unicode input viewport in src/tui/input.rs and use content rectangles in src/tui/widgets.rs. Test real terminal cursor coordinates across layouts and long inputs.
- [x] Add src/tui/dialog.rs for filterable choices, masked input, notices, cancellation and async execution. Keep existing TUI colors.
- [x] Refactor interactive setup in src/cli.rs to acquire the terminal before configuration and route all setup/auth UI through dialogs.
- [x] Route model/login through dialog effects in src/tui/mod.rs, swap validated streams in place, and route session resume through the backend/ACP client. Preserve chat on cancellation or error; block transitions during active turns.
- [x] Add regression coverage and a PTY smoke check for alternate-screen continuity, slash commands and model cancellation.
- [x] Bump Cargo.toml/Cargo.lock/README to beta 2 and add release notes. Run cargo fmt --all -- --check, cargo test --workspace --locked and cargo install --path .; verify lato --version.
- [ ] Merge into master, push master and the beta tag to github, and inspect CI/release results.

Validation: workspace tests, strict Clippy and formatting pass. The Unix PTY smoke verifies a single alternate-screen lifetime, retained model context, masked credentials, cancellation, sessions, doctor, first-run model configuration and folder trust.

Remote validation: master pushed as e3a70a0. Run 33722852718 failed Linux and Windows workspace tests and cancelled macOS. Earlier remote master also failed these jobs. Detailed logs require GitHub authentication, which is unavailable in this environment. Beta publication records this limitation instead of claiming cross-platform CI passed.
